	
		<% if (session[:img_num]).to_i == 5 %>
			<div id="test_image">
			<%= link_to image_tag('zebras.jpg'), '#' %>
			</div>
		<% elsif (session[:img_num]).to_i == 19 %>
			<div id="test_image">
			<%= link_to image_tag('car.jpg'), '#' %>
			</div>	
		<% end %>

		<%= render 'layouts/scaleACR' %>
        
		<div class="buttons">
			<%= link_to "Next", contentanswer_path, class: "btn btn-large btn-primary" %> 
		</div>
		
	
		
	